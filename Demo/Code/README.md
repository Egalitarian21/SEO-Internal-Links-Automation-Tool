# Internal Link Tool

Fresh project skeleton for internal link recommendation.

## Quick Start

```bash
cd Code
python -m pytest
python main.py --help
python main.py check-structure
python main.py init-brand --brand fitwarm
```

The old `app` package and legacy CLI are intentionally not included.
